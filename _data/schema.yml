# Master frame: this is not a legal validity check. It traces a rule from
# the design intent behind it through firm-level exposure to the point a
# company actually has to comply, independent of whether implementation
# gets delayed or staged along the way.
#
# Object of the rule and Convergence carry fixed types. Design Layer crosses
# two independent axes. Cost Layer carries no types, a matrix of firm-level
# variables, filled before any outcome is known.

object_of_the_rule:
  types: [attribute-based, relational, conduct-based]
  open_question: >
    What does the text name as its target: a measured attribute, a position
    in a network, or a future conduct condition?

design_layer:
  axes:
    scope: [scope-consistent, boundary-extending]
    trigger: [numeric, conduct]
  scope_trace:
    delegating_clause: >
      The statute or article that grants authority for this rulemaking,
      cited directly. What scope does it explicitly hand over?
    stated_purpose: >
      What the rulemaking body itself says it is doing: preamble, stated
      rationale, legislative history.
    operative_text_scope: >
      What the rule's own operative text actually reaches, read on its own,
      independent of the stated purpose.
    delta: >
      The gap between delegating_clause/stated_purpose and
      operative_text_scope. No gap points to scope-consistent. A gap that
      draws a new jurisdictional or definitional line points to
      boundary-extending.
  open_question: >
    Does this rulemaking sit inside the article's delegated authority or
    extend that boundary? Does it fire on a threshold/date or on an actor's
    conduct?

cost_layer:
  types: []
  variables: [distance from object, revenue share exposed, substitution available, prior positioning]
  open_question: >
    Firm by firm: how far from the named object, how much revenue is
    exposed, is substitution available, and is the firm already positioning
    before any outcome is known?

convergence:
  types: [direct hit, reallocation, amplification, diffusion]
  conduct_trigger_only: [latent, conditional reinstatement]
  test:
    - scale proportional to the trigger?
    - if proportional, does the landing point match the named target?
  open_question: >
    Reading the Cost Layer matrix against the two-question test above, which
    type does the outcome fall into?

method_note:
  statement: >
    The frame is provisional. A case may expose a distinction that requires
    revision of the shared vocabulary; the case is not required to fit a
    pre-fixed taxonomy.

# Closed-case construction set removed from the site. The working set is watchlist.
